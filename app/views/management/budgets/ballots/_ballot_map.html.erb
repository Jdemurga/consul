<div class="row margin-top">
  <div class="small-12 medium-3 column margin">
    <h4>Seleccione un barrio para realizar la votación</h4>
  </div>
  <div class="small-12 medium-6 column margin">
    <h2 class="sidebar-title">Barrios</h2>
    <br>
    <ul id="geozones" class="no-bullet">
      <% @budget.groups.each do |group| %>
          <li><%= link_to group.name, management_budget_ballot_path(@budget, @ballot, group_id: group.id), class: ("active" if group.id == params[:group]) %></li>
      <% end %>
    </ul>
  </div>


</div>